# Split by machine — this file is kept so old commands still find something.
#
#   Apple Silicon : requirements-mac.txt
#   NVIDIA        : requirements-cuda.txt   (read its header first; the torch
#                   wheel has to match your card's architecture)
#
# They differ in more than packaging: the two machines fail in opposite
# directions, and tools/backends.py holds the settings each one needs.
-r requirements-mac.txt
